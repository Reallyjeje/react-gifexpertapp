

const activo = true;

// let mensaje = 'si esta activo';

// if( !activo ) {
//     mensaje = 'si esta activo';
// } else {
//     mensaje = 'inactivo';
// }
// const mensaje = ( activo ) ? 'activo' : 'inactivo'
// const mensaje = ( !activo ) ? 'activo' : null;
const mensaje = (activo === true) && 'Activo'
console.log(mensaje);